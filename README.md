# 3d-project

C++20 FPS engine built with SDL2, bgfx, and EnTT. Targeting RE-style mansion environments.

## Setup

Requires `git lfs` — run `git lfs install` before cloning, or run `git lfs pull` after cloning to fetch the model files.
